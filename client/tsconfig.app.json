{
  "extends": "./tsconfig.json",  // Extend the main tsconfig
  "compilerOptions": {
    "outDir": "./dist",  // Set output directory for compiled code
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.app.tsbuildinfo"
  },
  "include": [
    "src/**/*"
  ]
}